# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.env

# Database
*.db
boutique.db

# Uploads
# Tracked on purpose: the seeded product images are referenced by seed_db.py
# and by the category/lookbook records, so a fresh clone needs them. Admin
# uploads land here too -- push those to Cloudinary rather than committing
# them if this directory starts growing.

# Node
node_modules/
dist/
build/
.npm/

# IDE
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
